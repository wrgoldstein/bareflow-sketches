* `bareflow.py` is the main module
* `compile.py` scans for files that are not itself or `bareflow.py`
* `example1.py` is an example

```
-> python compile.py
node: example1
edges in: ['public.people']
edges out: ['clean.people']
```
